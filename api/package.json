{
  "name": "api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot ./src/index.ts",
    "test:watch": "nodemon --config nodemon.json",
    "test": "bun test --watch"
  },
  "devDependencies": {
    "bun-types": "^1.0.7",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "hono": "^3.8.4"
  }
}
